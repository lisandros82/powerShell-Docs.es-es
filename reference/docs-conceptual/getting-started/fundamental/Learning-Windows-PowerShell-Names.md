---
ms.date: 2017-06-05
keywords: powershell,cmdlet
title: Aprendizaje de los nombres de Windows PowerShell
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: 28c821c4a617b6ac775dbdda8ade3d15c3f218c3
ms.sourcegitcommit: 598b7835046577841aea2211d613bb8513271a8b
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 06/08/2017
---
# <a name="learning-windows-powershell-names"></a><span data-ttu-id="878bb-103">Aprendizaje de los nombres de Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="878bb-103">Learning Windows PowerShell Names</span></span>
<span data-ttu-id="878bb-104">El aprendizaje de nombres de comandos y parámetros de comando supone una inversión de tiempo considerable con la mayoría de las interfaces de línea de comandos.</span><span class="sxs-lookup"><span data-stu-id="878bb-104">Learning names of commands and command parameters is a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="878bb-105">El problema es que hay muy pocos patrones, por lo que es la única manera de aprender consiste en memorizar cada comando y cada parámetro que necesite usar de forma regular.</span><span class="sxs-lookup"><span data-stu-id="878bb-105">The issue is that there are very few patterns, so the only way to learn is by memorizing each command and each parameter that you need to use on a regular basis.</span></span>

<span data-ttu-id="878bb-106">Cuando trabaja con un nuevo comando o parámetro, por lo general no puede usar lo que ya conoce; tendrá que buscar y aprender un nuevo nombre.</span><span class="sxs-lookup"><span data-stu-id="878bb-106">When you work with a new command or parameter, you cannot generally use what you already know; you have to find and learn a new name.</span></span> <span data-ttu-id="878bb-107">Si observa cómo crecen las interfaces de un pequeño conjunto de herramientas con las adiciones incrementales de funcionalidad, le resultará fácil ver por qué la estructura no es estándar.</span><span class="sxs-lookup"><span data-stu-id="878bb-107">If you look at how interfaces grow from a small set of tools with incremental additions to functionality, it is easy to see why the structure is nonstandard.</span></span> <span data-ttu-id="878bb-108">Con los nombres de comando especialmente, esto puede parecer lógico, ya que cada comando es una herramienta independiente, pero hay una manera mejor de gestionar los nombres de comando.</span><span class="sxs-lookup"><span data-stu-id="878bb-108">With command names in particular, this may sound logical since each command is a separate tool, but there is a better way to handle command names.</span></span>

<span data-ttu-id="878bb-109">La mayoría de los comandos se crea para administrar elementos del sistema operativo o las aplicaciones, como los servicios o los procesos.</span><span class="sxs-lookup"><span data-stu-id="878bb-109">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="878bb-110">Los comandos tienen una gran variedad de nombres que pueden o no encajar en una familia.</span><span class="sxs-lookup"><span data-stu-id="878bb-110">The commands have a variety of names that may or may not fit into a family.</span></span> <span data-ttu-id="878bb-111">Por ejemplo, en sistemas Windows, puede usar los comandos **net start** y **net stop** para iniciar y detener un servicio.</span><span class="sxs-lookup"><span data-stu-id="878bb-111">For example, on Windows systems, you can use the **net start** and **net stop** commands to start and stop a service.</span></span> <span data-ttu-id="878bb-112">Hay otra herramienta de control de servicio más generalizada para Windows que tiene un nombre completamente distinto, **sc**, que no encaja en el patrón de nomenclatura para los comandos de servicio **net**.</span><span class="sxs-lookup"><span data-stu-id="878bb-112">There is another more generalized service control tool for Windows that has a completely different name, **sc**, that does not fit into the naming pattern for the **net** service commands.</span></span> <span data-ttu-id="878bb-113">Para la administración del proceso, Windows tiene el comando **tasklist** para enumerar los procesos y el comando **taskkill** para eliminar los procesos.</span><span class="sxs-lookup"><span data-stu-id="878bb-113">For process management, Windows has the **tasklist** command to list processes and the **taskkill** command to kill processes.</span></span>

<span data-ttu-id="878bb-114">Los comandos que incluyen parámetros tienen especificaciones de parámetro irregulares.</span><span class="sxs-lookup"><span data-stu-id="878bb-114">Commands that take parameters have irregular parameter specifications.</span></span> <span data-ttu-id="878bb-115">No se puede usar el comando **net start** para iniciar un servicio en un equipo remoto.</span><span class="sxs-lookup"><span data-stu-id="878bb-115">You cannot use the **net start** command to start a service on a remote computer.</span></span> <span data-ttu-id="878bb-116">El comando **sc** iniciará un servicio en un equipo remoto, pero para especificar el equipo remoto, se debe anteponer una barra diagonal inversa doble a su nombre.</span><span class="sxs-lookup"><span data-stu-id="878bb-116">The **sc** command will start a service on a remote computer, but to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="878bb-117">Por ejemplo, para iniciar el servicio de administrador de trabajos en cola en un equipo remoto denominado DC01, debe escribir **sc \\\\DC01 start spooler**.</span><span class="sxs-lookup"><span data-stu-id="878bb-117">For example, to start the spooler service on a remote computer named DC01, you would type **sc \\\\DC01 start spooler**.</span></span> <span data-ttu-id="878bb-118">Para enumerar las tareas que se ejecutan en DC01, debe usar el parámetro **/S** (para "system") y proporcionar el nombre DC01 sin barras diagonales inversas: **tasklist /S DC01**.</span><span class="sxs-lookup"><span data-stu-id="878bb-118">To list tasks running on DC01, you need to use the **/S** (for "system") parameter and supply the name DC01 without backslashes, like this: **tasklist /S DC01**.</span></span>

<span data-ttu-id="878bb-119">Aunque existen importantes diferencias técnicas entre un servicio y un proceso, ambos son ejemplos de elementos fáciles de administrar en un equipo con un ciclo de vida bien definido.</span><span class="sxs-lookup"><span data-stu-id="878bb-119">Although there are important technical distinctions between a service and a process, they are both examples of manageable elements on a computer that have a well-defined life cycle.</span></span> <span data-ttu-id="878bb-120">Es posible que quiera iniciar o detener un servicio o un proceso, u obtener una lista de todos los servicios o procesos que se están ejecutando actualmente.</span><span class="sxs-lookup"><span data-stu-id="878bb-120">You may want to start or stop a service or process, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="878bb-121">En otras palabras, aunque un servicio y un proceso son cosas diferentes, las acciones que realizamos en un servicio o un proceso suelen ser conceptualmente iguales.</span><span class="sxs-lookup"><span data-stu-id="878bb-121">In other words, although a service and a process are different things, the actions we perform on a service or a process are often conceptually the same.</span></span> <span data-ttu-id="878bb-122">Además, las selecciones que realizamos para personalizar una acción mediante la especificación de parámetros también pueden ser conceptualmente similares.</span><span class="sxs-lookup"><span data-stu-id="878bb-122">Furthermore, choices we may make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="878bb-123">Windows PowerShell aprovecha estas similitudes para reducir el número de nombres distintos que debe saber para entender y usar los cmdlets.</span><span class="sxs-lookup"><span data-stu-id="878bb-123">Windows PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

### <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="878bb-124">Los cmdlets usan nombres verbo-sustantivo para reducir la memorización de comandos</span><span class="sxs-lookup"><span data-stu-id="878bb-124">Cmdlets Use Verb-Noun Names to Reduce Command Memorization</span></span>
<span data-ttu-id="878bb-125">Windows PowerShell usa un sistema de nomenclatura "verbo-sustantivo", donde cada nombre de cmdlet consta de un verbo estándar seguido de un guión y un sustantivo específico.</span><span class="sxs-lookup"><span data-stu-id="878bb-125">Windows PowerShell uses a "verb-noun" naming system, where each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="878bb-126">Los verbos de Windows PowerShell no son siempre verbos ingleses, pero expresan acciones específicas en Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="878bb-126">Windows PowerShell verbs are not always English verbs, but they express specific actions in Windows PowerShell.</span></span> <span data-ttu-id="878bb-127">Los sustantivos son muy similares a los de cualquier idioma, describen tipos específicos de objetos que son importantes para la administración del sistema.</span><span class="sxs-lookup"><span data-stu-id="878bb-127">Nouns are very much like nouns in any language, they describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="878bb-128">Es fácil demostrar cómo estos nombres de dos componentes reducen el esfuerzo de aprendizaje examinando algunos ejemplos de verbos y sustantivos.</span><span class="sxs-lookup"><span data-stu-id="878bb-128">It is easy to demonstrate how these two-part names reduce learning effort by looking at a few examples of verbs and nouns.</span></span>

<span data-ttu-id="878bb-129">Los sustantivos tienen menos restricciones, pero siempre que deben describir sobre qué elemento actúa un comando.</span><span class="sxs-lookup"><span data-stu-id="878bb-129">Nouns are less restricted, but they should always describe what a command acts upon.</span></span> <span data-ttu-id="878bb-130">Windows PowerShell presenta comandos, como **Get-Process**, **Stop-Process**, **Get-Service** y **Stop-Service**.</span><span class="sxs-lookup"><span data-stu-id="878bb-130">Windows PowerShell has commands such as **Get-Process**, **Stop-Process**, **Get-Service**, and **Stop-Service**.</span></span>

<span data-ttu-id="878bb-131">En el caso de dos sustantivos y dos verbos, la coherencia no simplifica tanto el aprendizaje.</span><span class="sxs-lookup"><span data-stu-id="878bb-131">In the case of two nouns and two verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="878bb-132">Sin embargo, si observa un conjunto estándar de 10 verbos y 10 sustantivos, solo tiene que comprender 20 términos, aunque estos se pueden usar para formar hasta 100 nombres de comando distintos.</span><span class="sxs-lookup"><span data-stu-id="878bb-132">However, if you look at a standard set of 10 verbs and 10 nouns, you then have only 20 words to understand, but those words can be used to form 100 distinct command names.</span></span>

<span data-ttu-id="878bb-133">Con frecuencia, puede reconocer lo que hace un comando mediante la lectura de su nombre. También suele ser bastante evidente el nombre que debe usarse para un comando nuevo.</span><span class="sxs-lookup"><span data-stu-id="878bb-133">Frequently, you can recognize what a command does by reading its name, and it is usually apparent what name should be used for a new command.</span></span> <span data-ttu-id="878bb-134">Por ejemplo, un comando para apagar el equipo podría ser **Stop-Computer**.</span><span class="sxs-lookup"><span data-stu-id="878bb-134">For example, a computer shutdown command might be **Stop-Computer**.</span></span> <span data-ttu-id="878bb-135">Una comando para enumerar todos los equipos en una red podría ser **Get-Computer**.</span><span class="sxs-lookup"><span data-stu-id="878bb-135">A command that lists all computers on a network might be **Get-Computer**.</span></span> <span data-ttu-id="878bb-136">El comando que obtiene la fecha del sistema es **Get-Date**.</span><span class="sxs-lookup"><span data-stu-id="878bb-136">The command that gets the system date is **Get-Date**.</span></span>

<span data-ttu-id="878bb-137">Puede enumerar todos los comandos que incluyan un verbo determinado con el parámetro **-Verb** para **Get-Command** (hablaremos de **Get-Command** con más detalle en la siguiente sección).</span><span class="sxs-lookup"><span data-stu-id="878bb-137">You can list all commands that include a particular verb with the **-Verb** parameter for **Get-Command** (We will discuss **Get-Command** in detail in the next section).</span></span> <span data-ttu-id="878bb-138">Por ejemplo, para ver todos los cmdlets que usan el verbo **Get**, escriba:</span><span class="sxs-lookup"><span data-stu-id="878bb-138">For example, to see all cmdlets that use the verb **Get**, type:</span></span>

```
PS> Get-Command -Verb Get
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="878bb-139">El parámetro **-Noun** es aún más útil, porque permite ver una familia de comandos que afectan al mismo tipo de objeto.</span><span class="sxs-lookup"><span data-stu-id="878bb-139">The **-Noun** parameter is even more useful because it allows you to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="878bb-140">Por ejemplo, si desea ver qué comandos están disponibles para administrar los servicios, escriba el siguiente comando:</span><span class="sxs-lookup"><span data-stu-id="878bb-140">For example, if you want to see which commands are available for managing services, type following command:</span></span>

```
PS> Get-Command -Noun Service
CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str... 
...
```

<span data-ttu-id="878bb-141">Un comando no es necesariamente un cmdlet, simplemente porque tiene un esquema de nomenclatura de verbo-sustantivo.</span><span class="sxs-lookup"><span data-stu-id="878bb-141">A command is not necessarily a cmdlet, just because it has a verb-noun naming scheme.</span></span> <span data-ttu-id="878bb-142">Un ejemplo de comando de Windows PowerShell nativo que no es un cmdlet, pero tiene un nombre verbo-sustantivo, es el comando para borrar una ventana de la consola, Clear-Host.</span><span class="sxs-lookup"><span data-stu-id="878bb-142">One example of a native Windows PowerShell command that is not a cmdlet but has a verb-noun name, is the command for clearing a console window, Clear-Host.</span></span> <span data-ttu-id="878bb-143">El comando Clear-Host es en realidad una función interna, como puede ver si ejecuta Get-Command en él:</span><span class="sxs-lookup"><span data-stu-id="878bb-143">The Clear-Host command is actually an internal function, as you can see if you run Get-Command against it:</span></span>

```
PS> Get-Command -Name Clear-Host

CommandType     Name                            Definition
-----------     ----                            ----------
Function        Clear-Host                      $spaceType = [System.Managem...
```

### <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="878bb-144">Los cmdlets usan parámetros estándar</span><span class="sxs-lookup"><span data-stu-id="878bb-144">Cmdlets Use Standard Parameters</span></span>
<span data-ttu-id="878bb-145">Como hemos indicado anteriormente, los comandos usados en las interfaces de línea de comandos tradicionales no suelen tener nombres de parámetro coherentes.</span><span class="sxs-lookup"><span data-stu-id="878bb-145">As noted earlier, commands used in traditional command-line interfaces do not generally have consistent parameter names.</span></span> <span data-ttu-id="878bb-146">En ocasiones, los parámetros no tienen ningún nombre.</span><span class="sxs-lookup"><span data-stu-id="878bb-146">Sometimes parameters do not have names at all.</span></span> <span data-ttu-id="878bb-147">Si lo tienen, suele tener un único carácter o una palabra abreviada que se pueda escribir rápidamente, pero que los nuevos usuarios no puedan comprender fácilmente.</span><span class="sxs-lookup"><span data-stu-id="878bb-147">When they do, they are often single-character or abbreviated words that can be typed rapidly but are not easily understood by new users.</span></span>

<span data-ttu-id="878bb-148">A diferencia de la mayoría de interfaces de línea de comandos tradicionales, Windows PowerShell procesa los parámetros directamente y usa este acceso directo a los parámetros junto con instrucciones para desarrolladores para estandarizar los nombres de parámetro.</span><span class="sxs-lookup"><span data-stu-id="878bb-148">Unlike most other traditional command-line interfaces, Windows PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="878bb-149">Aunque esto no garantiza que todos los cmdlets cumplan siempre con los estándares, es un procedimiento recomendado.</span><span class="sxs-lookup"><span data-stu-id="878bb-149">Although this does not guarantee that every cmdlet will always conform to the standards, it does encourage it.</span></span>

> [!NOTE]
> <span data-ttu-id="878bb-150">Los nombres de parámetro siempre tienen un '-' antepuesto cuando los usa, para permitir que Windows PowerShell los identifique claramente como parámetros.</span><span class="sxs-lookup"><span data-stu-id="878bb-150">Parameter names always have a '-' prepended to them when you use them, to allow Windows PowerShell to clearly identify them as parameters.</span></span> <span data-ttu-id="878bb-151">En el ejemplo **Get-Command -Name Clear-Host**, el nombre del parámetro es **Name**, pero se escribe como -**Name**.</span><span class="sxs-lookup"><span data-stu-id="878bb-151">In the **Get-Command -Name Clear-Host** example, the parameter's name is **Name**, but it is entered as -**Name**.</span></span>

<span data-ttu-id="878bb-152">Estas son algunas de las características generales de los nombres y usos de los parámetros estándar.</span><span class="sxs-lookup"><span data-stu-id="878bb-152">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

#### <a name="the-help-parameter-"></a><span data-ttu-id="878bb-153">Parámetro de ayuda (?)</span><span class="sxs-lookup"><span data-stu-id="878bb-153">The Help Parameter (?)</span></span>
<span data-ttu-id="878bb-154">Si especifica el parámetro **-?**</span><span class="sxs-lookup"><span data-stu-id="878bb-154">When you specify the **-?**</span></span> <span data-ttu-id="878bb-155">para cualquier cmdlet, el cmdlet no se ejecuta.</span><span class="sxs-lookup"><span data-stu-id="878bb-155">parameter to any cmdlet, the cmdlet is not executed.</span></span> <span data-ttu-id="878bb-156">En su lugar, Windows PowerShell muestra la ayuda del cmdlet.</span><span class="sxs-lookup"><span data-stu-id="878bb-156">Instead, Windows PowerShell displays help for the cmdlet.</span></span>

#### <a name="common-parameters"></a><span data-ttu-id="878bb-157">Parámetros comunes</span><span class="sxs-lookup"><span data-stu-id="878bb-157">Common Parameters</span></span>
<span data-ttu-id="878bb-158">Windows PowerShell presenta varios parámetros que se conocen como *parámetros comunes*.</span><span class="sxs-lookup"><span data-stu-id="878bb-158">Windows PowerShell has several parameters known as *common parameters*.</span></span> <span data-ttu-id="878bb-159">Dado que estos parámetros los controla el motor de Windows PowerShell, se comportarán del mismo modo siempre que cmdlet los implemente.</span><span class="sxs-lookup"><span data-stu-id="878bb-159">Because these parameters are controlled by the Windows PowerShell engine, whenever they are implemented by a cmdlet, they will always behave the same way.</span></span> <span data-ttu-id="878bb-160">Los parámetros comunes son **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable** y **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="878bb-160">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

#### <a name="suggested-parameters"></a><span data-ttu-id="878bb-161">Parámetros sugeridos</span><span class="sxs-lookup"><span data-stu-id="878bb-161">Suggested Parameters</span></span>
<span data-ttu-id="878bb-162">Los cmdlets principales de Windows PowerShell usan nombres estándar para parámetros similares.</span><span class="sxs-lookup"><span data-stu-id="878bb-162">The Windows PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="878bb-163">Aunque no se exige el uso de nombres de parámetro, existen un instrucciones explícitas para usarlos a fin de promover la estandarización.</span><span class="sxs-lookup"><span data-stu-id="878bb-163">Although the use of parameter names is not enforced, there is explicit guidance for usage to encourage standardization.</span></span>

<span data-ttu-id="878bb-164">Por ejemplo, las instrucciones recomiendan denominar un parámetro que haga referencia a un equipo por su nombre, como **nombreDeEquipo**, en lugar de servidor, host, sistema, nodo u otras palabras alternativas comunes.</span><span class="sxs-lookup"><span data-stu-id="878bb-164">For example, the guidance recommends naming a parameter that refers to a computer by name as **ComputerName**, rather than Server, Host, System, Node, or other common alternative words.</span></span> <span data-ttu-id="878bb-165">Entre los nombres de parámetros importantes sugeridos se encuentran **Force**, **Exclude**, **Include**, **PassThru**, **Path** y **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="878bb-165">Among the important suggested parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>

