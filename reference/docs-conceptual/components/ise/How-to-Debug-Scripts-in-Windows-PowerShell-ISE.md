---
ms.date: 01/02/2020
keywords: powershell, cmdlet
title: Cómo depurar scripts en ISE de Windows PowerShell
ms.openlocfilehash: c5da80f3e0e013448533c80bbe1957a301be38f5
ms.sourcegitcommit: 058a6e86eac1b27ca57a11687019df98709ed709
ms.translationtype: HT
ms.contentlocale: es-ES
ms.lasthandoff: 01/08/2020
ms.locfileid: "75737124"
---
# <a name="how-to-debug-scripts-in-windows-powershell-ise"></a><span data-ttu-id="c795d-103">Cómo depurar scripts en ISE de Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="c795d-103">How to Debug Scripts in Windows PowerShell ISE</span></span>

<span data-ttu-id="c795d-104">En este artículo se describe cómo depurar scripts en un equipo local mediante las características de depuración visual del Entorno de scripting integrado (ISE) de Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="c795d-104">This article describes how to debug scripts on a local computer by using the Windows PowerShell Integrated Scripting Environment (ISE) visual debugging features.</span></span>

## <a name="how-to-manage-breakpoints"></a><span data-ttu-id="c795d-105">Como administrar los puntos de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-105">How to manage breakpoints</span></span>

<span data-ttu-id="c795d-106">Un punto de interrupción es una zona designada en un script que desee que la operación entre en pausa para poder examinar el estado actual de las variables y el entorno en que se ejecuta el script.</span><span class="sxs-lookup"><span data-stu-id="c795d-106">A breakpoint is a designated spot in a script where you would like operation to pause so that you can examine the current state of the variables and the environment in which your script is running.</span></span>
<span data-ttu-id="c795d-107">Cuando un punto de interrupción pausa un script, puede ejecutar comandos en el panel de consola para examinar el estado del script.</span><span class="sxs-lookup"><span data-stu-id="c795d-107">Once your script is paused by a breakpoint, you can run commands in the Console Pane to examine the state of your script.</span></span> <span data-ttu-id="c795d-108">Puede generar variables o ejecutar otros comandos.</span><span class="sxs-lookup"><span data-stu-id="c795d-108">You can output variables or run other commands.</span></span> <span data-ttu-id="c795d-109">Incluso puede modificar el valor de las variables que son visibles para el contexto del script que se está ejecutando.</span><span class="sxs-lookup"><span data-stu-id="c795d-109">You can even modify the value of any variables that are visible to the context of the currently running script.</span></span> <span data-ttu-id="c795d-110">Después de examinar lo que desea ver, puede reanudar la operación del script.</span><span class="sxs-lookup"><span data-stu-id="c795d-110">After you have examined what you want to see, you can resume operation of the script.</span></span>

<span data-ttu-id="c795d-111">Puede establecer tres tipos de puntos de interrupción en el entorno de depuración de Windows PowerShell:</span><span class="sxs-lookup"><span data-stu-id="c795d-111">You can set three types of breakpoints in the Windows PowerShell debugging environment:</span></span>

1. <span data-ttu-id="c795d-112">**Punto de interrupción de línea**.</span><span class="sxs-lookup"><span data-stu-id="c795d-112">**Line breakpoint**.</span></span> <span data-ttu-id="c795d-113">El script se pausa cuando se alcanza la línea designada durante la operación del script.</span><span class="sxs-lookup"><span data-stu-id="c795d-113">The script pauses when the designated line is reached during the operation of the script</span></span>

1. <span data-ttu-id="c795d-114">**Variable breakpoint.**</span><span class="sxs-lookup"><span data-stu-id="c795d-114">**Variable breakpoint.**</span></span> <span data-ttu-id="c795d-115">El script se pausa cuando cambia el valor de la variable designada.</span><span class="sxs-lookup"><span data-stu-id="c795d-115">The script pauses whenever the designated variable's value changes.</span></span>

1. <span data-ttu-id="c795d-116">**Command breakpoint.**</span><span class="sxs-lookup"><span data-stu-id="c795d-116">**Command breakpoint.**</span></span> <span data-ttu-id="c795d-117">El script se pausa cada vez que el comando designado está a punto de ejecutarse durante la operación del script.</span><span class="sxs-lookup"><span data-stu-id="c795d-117">The script pauses whenever the designated command is about to be run during the operation of the script.</span></span> <span data-ttu-id="c795d-118">Puede incluir parámetros para filtrar aún más el punto de interrupción solo para operación deseada.</span><span class="sxs-lookup"><span data-stu-id="c795d-118">It can include parameters to further filter the breakpoint to only the operation you want.</span></span> <span data-ttu-id="c795d-119">El comando también puede ser una función que ha creado.</span><span class="sxs-lookup"><span data-stu-id="c795d-119">The command can also be a function you created.</span></span>

<span data-ttu-id="c795d-120">De estos, en el entorno de depuración Windows PowerShell ISE, solo se pueden establecer puntos de interrupción de línea usando el menú o los métodos abreviados de teclado.</span><span class="sxs-lookup"><span data-stu-id="c795d-120">Of these, in the Windows PowerShell ISE debugging environment, only line breakpoints can be set by using the menu or the keyboard shortcuts.</span></span> <span data-ttu-id="c795d-121">Los otros dos tipos de puntos de interrupción se pueden establecer, pero debe hacerse desde el panel de consola mediante el cmdlet [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-121">The other two types of breakpoints can be set, but they are set from the Console Pane by using the [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md) cmdlet.</span></span> <span data-ttu-id="c795d-122">En esta sección se describe cómo puede realizar la depuración de tareas en Windows PowerShell ISE mediante los menús, cuando están disponibles, y ejecutar una serie más amplia de comandos desde el panel de consola usando scripts.</span><span class="sxs-lookup"><span data-stu-id="c795d-122">This section describes how you can perform debugging tasks in Windows PowerShell ISE by using the menus where available, and perform a wider range of commands from the Console Pane by using scripting.</span></span>

### <a name="to-set-a-breakpoint"></a><span data-ttu-id="c795d-123">Para establecer un punto de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-123">To set a breakpoint</span></span>

<span data-ttu-id="c795d-124">Solo se puede establecer un punto de interrupción en un script después de guardarlo.</span><span class="sxs-lookup"><span data-stu-id="c795d-124">A breakpoint can be set in a script only after it has been saved.</span></span> <span data-ttu-id="c795d-125">Haga clic con el botón derecho en la línea donde desee establecer un punto de interrupción y después haga clic en **Alternar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-125">Right-click the line where you want to set a line breakpoint, and then click **Toggle Breakpoint**.</span></span> <span data-ttu-id="c795d-126">O bien, haga clic en la línea donde desee establecer un punto de interrupción y presione <kbd>F9</kbd> o, en el menú **Depurar**, haga clic en **Alternar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-126">Or, click the line where you want to set a line breakpoint, and press <kbd>F9</kbd> or, on the **Debug** menu, click **Toggle Breakpoint**.</span></span>

<span data-ttu-id="c795d-127">El script siguiente es un ejemplo de cómo establecer un punto de interrupción de variable desde el panel de consola mediante el cmdlet [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-127">The following script is an example of how you can set a variable breakpoint from the Console Pane by using the [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command sets a breakpoint on the Server variable in the Sample.ps1 script.
Set-PSBreakpoint -Script sample.ps1 -Variable Server
```

### <a name="list-all-breakpoints"></a><span data-ttu-id="c795d-128">Enumerar todos los puntos de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-128">List all breakpoints</span></span>

<span data-ttu-id="c795d-129">Muestra todos los puntos de interrupción de la sesión actual de Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="c795d-129">Displays all breakpoints in the current Windows PowerShell session.</span></span>

<span data-ttu-id="c795d-130">En el menú **Depurar**, haga clic en **Mostrar puntos de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-130">On the **Debug** menu, click **List Breakpoints**.</span></span> <span data-ttu-id="c795d-131">El script siguiente es un ejemplo de cómo enumerar todos los puntos de interrupción desde el panel de consola mediante el cmdlet [Get-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Get-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-131">The following script is an example of how you can list all breakpoints from the Console Pane by using the [Get-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Get-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command lists all breakpoints in the current session.
Get-PSBreakpoint
```

### <a name="remove-a-breakpoint"></a><span data-ttu-id="c795d-132">Quitar un punto de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-132">Remove a breakpoint</span></span>

<span data-ttu-id="c795d-133">Al quitar un punto de interrupción, este se elimina.</span><span class="sxs-lookup"><span data-stu-id="c795d-133">Removing a breakpoint deletes it.</span></span>

<span data-ttu-id="c795d-134">Si piensa que podría querer usarlo más adelante, considere la posibilidad de [deshabilitar un punto de interrupción](#disable-a-breakpoint) en su lugar.</span><span class="sxs-lookup"><span data-stu-id="c795d-134">If you think you might want to use it again later, consider [Disable a Breakpoint](#disable-a-breakpoint) it instead.</span></span> <span data-ttu-id="c795d-135">Haga clic con el botón derecho en la línea donde desee quitar un punto de interrupción y después haga clic en **ToggleBreakpoint**.</span><span class="sxs-lookup"><span data-stu-id="c795d-135">Right-click the line where you want to remove a breakpoint, and then click **ToggleBreakpoint**.</span></span>
<span data-ttu-id="c795d-136">O bien, haga clic en la línea donde desee quitar un punto de interrupción y, en el menú **Depurar**, haga clic en **Alternar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-136">Or, click the line where you want to remove a breakpoint, and on the **Debug** menu, click **Toggle Breakpoint**.</span></span> <span data-ttu-id="c795d-137">El script siguiente es un ejemplo de cómo quitar un punto de interrupción con un identificador especificado en el panel de consola mediante el cmdlet [Remove-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Remove-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-137">The following script is an example of how to remove a breakpoint with a specified ID from the Console Pane by using the [Remove-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Remove-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command deletes the breakpoint with breakpoint ID 2.
Remove-PSBreakpoint -Id 2
```

### <a name="remove-all-breakpoints"></a><span data-ttu-id="c795d-138">Quitar todos los puntos de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-138">Remove All Breakpoints</span></span>

<span data-ttu-id="c795d-139">Para quitar todos los puntos de interrupción definidos en la sesión actual, en el menú **Depurar**, haga clic en **Quitar todos los puntos de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-139">To remove all breakpoints defined in the current session, on the **Debug** menu, click **Remove All Breakpoints**.</span></span>

<span data-ttu-id="c795d-140">El script siguiente es un ejemplo de cómo quitar todos los puntos de interrupción del panel de consola mediante el cmdlet [Remove-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Remove-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-140">The following script is an example of how to remove all breakpoints from the Console Pane by using the [Remove-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Remove-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command deletes all of the breakpoints in the current session.
Get-PSBreakpoint | Remove-PSBreakpoint
```

### <a name="disable-a-breakpoint"></a><span data-ttu-id="c795d-141">Deshabilitar un punto de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-141">Disable a Breakpoint</span></span>

<span data-ttu-id="c795d-142">Al deshabilitar un punto de interrupción, este no se quita; permanece desactivado hasta que se vuelve a habilitar.</span><span class="sxs-lookup"><span data-stu-id="c795d-142">Disabling a breakpoint does not remove it; it turns it off until it is enabled.</span></span> <span data-ttu-id="c795d-143">Para deshabilitar un punto de interrupción de línea específico, haga clic con el botón derecho en la línea donde desee deshabilitar un punto de interrupción y luego haga clic en **Deshabilitar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-143">To disable a specific line breakpoint, right-click the line where you want to disable a breakpoint, and then click **Disable Breakpoint**.</span></span> <span data-ttu-id="c795d-144">O bien, haga clic en la línea donde desee deshabilitar un punto de interrupción y presione <kbd>F9</kbd> o, en el menú **Depurar**, haga clic en **Deshabilitar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-144">Or, click the line where you want to disable a breakpoint, and press <kbd>F9</kbd> or, on the **Debug** menu, click **Disable Breakpoint**.</span></span> <span data-ttu-id="c795d-145">El script siguiente es un ejemplo de cómo quitar un punto de interrupción con un identificador especificado del panel de consola mediante el cmdlet [Disable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Disable-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-145">The following script is an example of how you can remove a breakpoint with a specified ID from the Console Pane by using the [Disable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Disable-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command disables the breakpoint with breakpoint ID 0.
Disable-PSBreakpoint -Id 0
```

### <a name="disable-all-breakpoints"></a><span data-ttu-id="c795d-146">Deshabilitar todos los puntos de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-146">Disable All Breakpoints</span></span>

<span data-ttu-id="c795d-147">Al deshabilitar un punto de interrupción, este no se quita; permanece desactivado hasta que se vuelve a habilitar.</span><span class="sxs-lookup"><span data-stu-id="c795d-147">Disabling a breakpoint does not remove it; it turns it off until it is enabled.</span></span> <span data-ttu-id="c795d-148">Para deshabilitar todos los puntos de interrupción de la sesión actual, en el menú **Depurar**, haga clic en **Deshabilitar todos los puntos de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-148">To disable all breakpoints in the current session, on the **Debug** menu, click **Disable all Breakpoints**.</span></span> <span data-ttu-id="c795d-149">El script siguiente es un ejemplo de cómo deshabilitar todos los puntos de interrupción desde el panel de consola mediante el cmdlet [Disable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Disable-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-149">The following script is an example of how you can disable all breakpoints from the Console Pane by using the [Disable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Disable-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command disables all breakpoints in the current session.
# You can abbreviate this command as: "gbp | dbp".
Get-PSBreakpoint | Disable-PSBreakpoint
```

### <a name="enable-a-breakpoint"></a><span data-ttu-id="c795d-150">Habilitar un punto de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-150">Enable a Breakpoint</span></span>

<span data-ttu-id="c795d-151">Para habilitar un punto de interrupción específico, haga clic con el botón derecho en la línea donde desee habilitar un punto de interrupción y después haga clic en **Habilitar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-151">To enable a specific breakpoint, right-click the line where you want to enable a breakpoint, and then click **Enable Breakpoint**.</span></span> <span data-ttu-id="c795d-152">O bien, haga clic en la línea donde desee habilitar un punto de interrupción y presione <kbd>F9</kbd> o, en el menú **Depurar**, haga clic en **Habilitar punto de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-152">Or, click the line where you want to enable a breakpoint, and then press <kbd>F9</kbd> or, on the **Debug** menu, click **Enable Breakpoint**.</span></span> <span data-ttu-id="c795d-153">El script siguiente es un ejemplo de cómo habilitar puntos de interrupción desde el panel de consola mediante el cmdlet [Enable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Enable-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-153">The following script is an example of how you can enable specific breakpoints from the Console Pane by using the [Enable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Enable-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command enables breakpoints with breakpoint IDs 0, 1, and 5.
Enable-PSBreakpoint -Id 0, 1, 5
```

### <a name="enable-all-breakpoints"></a><span data-ttu-id="c795d-154">Habilitar todos los puntos de interrupción</span><span class="sxs-lookup"><span data-stu-id="c795d-154">Enable All Breakpoints</span></span>

<span data-ttu-id="c795d-155">Para habilitar todos los puntos de interrupción definidos en la sesión actual, en el menú **Depurar**, haga clic en **Habilitar todos los puntos de interrupción**.</span><span class="sxs-lookup"><span data-stu-id="c795d-155">To enable all breakpoints defined in the current session, on the **Debug** menu, click **Enable all Breakpoints**.</span></span> <span data-ttu-id="c795d-156">El script siguiente es un ejemplo de cómo habilitar todos los puntos de interrupción desde el panel de consola mediante el cmdlet [Enable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Enable-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-156">The following script is an example of how you can enable all breakpoints from the Console Pane by using the [Enable-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Enable-PSBreakpoint.md) cmdlet.</span></span>

```powershell
# This command enables all breakpoints in the current session.
# You can abbreviate the command by using their aliases: "gbp | ebp".
Get-PSBreakpoint | Enable-PSBreakpoint
```

## <a name="how-to-manage-a-debugging-session"></a><span data-ttu-id="c795d-157">Cómo administrar una sesión de depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-157">How to manage a debugging session</span></span>

<span data-ttu-id="c795d-158">Antes de iniciar la depuración, debe establecer uno o varios puntos de interrupción.</span><span class="sxs-lookup"><span data-stu-id="c795d-158">Before you start debugging, you must set one or more breakpoints.</span></span> <span data-ttu-id="c795d-159">No se puede establecer un punto de interrupción si no se guarda el script que desea depurar.</span><span class="sxs-lookup"><span data-stu-id="c795d-159">You cannot set a breakpoint unless the script that you want to debug is saved.</span></span> <span data-ttu-id="c795d-160">Para obtener instrucciones acerca de cómo establecer un punto de interrupción, vea [Cómo administrar los puntos de interrupción](#how-to-manage-breakpoints) o [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md).</span><span class="sxs-lookup"><span data-stu-id="c795d-160">For directions on of how to set a breakpoint, see [How to manage breakpoints](#how-to-manage-breakpoints) or [Set-PSBreakpoint](/reference/6/Microsoft.PowerShell.Utility/Set-PSBreakpoint.md).</span></span>
<span data-ttu-id="c795d-161">Después de iniciar la depuración, no se puede editar un script hasta que la depuración se detenga.</span><span class="sxs-lookup"><span data-stu-id="c795d-161">After you start debugging, you cannot edit a script until you stop debugging.</span></span> <span data-ttu-id="c795d-162">Un script con uno o más puntos de interrupción establecidos se guarda automáticamente antes de ejecutarse.</span><span class="sxs-lookup"><span data-stu-id="c795d-162">A script that has one or more breakpoints set is automatically saved before it is run.</span></span>

### <a name="to-start-debugging"></a><span data-ttu-id="c795d-163">Para iniciar la depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-163">To start debugging</span></span>

<span data-ttu-id="c795d-164">Presione <kbd>F5</kbd> o haga clic en el icono **Ejecutar script** en la barra de herramientas, o bien, en el menú **Depurar**, haga clic en **Ejecutar o continuar**.</span><span class="sxs-lookup"><span data-stu-id="c795d-164">Press <kbd>F5</kbd> or, on the toolbar, click the **Run Script** icon, or on the **Debug** menu click **Run/Continue**.</span></span> <span data-ttu-id="c795d-165">El script se ejecuta hasta que encuentra el primer punto de interrupción.</span><span class="sxs-lookup"><span data-stu-id="c795d-165">The script runs until it encounters the first breakpoint.</span></span> <span data-ttu-id="c795d-166">Detiene la operación en este punto y resalta la línea en la que se produce la pausa.</span><span class="sxs-lookup"><span data-stu-id="c795d-166">It pauses operation there and highlights the line on which it paused.</span></span>

### <a name="to-continue-debugging"></a><span data-ttu-id="c795d-167">Para continuar con la depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-167">To continue debugging</span></span>

<span data-ttu-id="c795d-168">Presione <kbd>F5</kbd> o haga clic en el icono **Ejecutar Script** en la barra de herramientas, o bien, en el menú **Depurar**, haga clic en **Ejecutar o continuar**. También puede escribir `C` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-168">Press <kbd>F5</kbd> or, on the toolbar, click the **Run Script** icon, or on the **Debug** menu, click **Run/Continue** or, in the Console Pane, type `C` and then press <kbd>ENTER</kbd>.</span></span> <span data-ttu-id="c795d-169">Esto hace que el script se siga ejecutando hasta el punto de interrupción siguiente o hasta el final si no se encuentran más puntos de interrupción.</span><span class="sxs-lookup"><span data-stu-id="c795d-169">This causes the script to continue running to the next breakpoint or to the end of the script if no further breakpoints are encountered.</span></span>

### <a name="to-view-the-call-stack"></a><span data-ttu-id="c795d-170">Para ver la pila de llamadas</span><span class="sxs-lookup"><span data-stu-id="c795d-170">To view the call stack</span></span>

<span data-ttu-id="c795d-171">La pila de llamadas muestra la ubicación de ejecución actual en el script.</span><span class="sxs-lookup"><span data-stu-id="c795d-171">The call stack displays the current run location in the script.</span></span> <span data-ttu-id="c795d-172">Si el script se ejecuta en una función que llamó una función diferente, se representa mediante filas adicionales en la salida.</span><span class="sxs-lookup"><span data-stu-id="c795d-172">If the script is running in a function that was called by a different function, then that is represented in the display by additional rows in the output.</span></span> <span data-ttu-id="c795d-173">La última fila muestra el script original y la línea en la que se llamó a una función.</span><span class="sxs-lookup"><span data-stu-id="c795d-173">The bottom-most row displays the original script and the line in it in which a function was called.</span></span> <span data-ttu-id="c795d-174">La siguiente línea muestra esa función y la línea en la que se podría haber llamado a otra función.</span><span class="sxs-lookup"><span data-stu-id="c795d-174">The next line up shows that function and the line in it in which another function might have been called.</span></span> <span data-ttu-id="c795d-175">La primera fila muestra el contexto actual de la línea actual en la que se estableció el punto de interrupción.</span><span class="sxs-lookup"><span data-stu-id="c795d-175">The top-most row shows the current context of the current line on which the breakpoint is set.</span></span>

<span data-ttu-id="c795d-176">Mientras está en pausa, para ver la pila de llamadas actual, presione <kbd>CTRL</kbd>+<kbd>MAYÚS</kbd>+<kbd>D</kbd> o, en el menú **Depurar**, haga clic en **Mostrar pila de llamadas**. También puede escribir `K` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-176">While paused, to see the current call stack, press <kbd>CTRL</kbd>+<kbd>SHIFT</kbd>+<kbd>D</kbd> or, on the **Debug** menu, click **Display Call Stack** or, in the Console Pane, type `K` and then press <kbd>ENTER</kbd>.</span></span>

### <a name="to-stop-debugging"></a><span data-ttu-id="c795d-177">Para detener la depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-177">To stop debugging</span></span>

<span data-ttu-id="c795d-178">Presione <kbd>MAYÚS</kbd>+<kbd>F5</kbd> o, en el menú **Depurar**, haga clic en **Detener el depurador**. También puede escribir `Q` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-178">Press <kbd>SHIFT</kbd>+<kbd>F5</kbd> or, on the **Debug** menu, click **Stop Debugger**, or, in the Console Pane, type `Q` and then press <kbd>ENTER</kbd>.</span></span>

## <a name="how-to-step-over-step-into-and-step-out-while-debugging"></a><span data-ttu-id="c795d-179">Cómo depurar paso a paso por procedimientos, por instrucciones y para salir durante la depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-179">How to step over, step into, and step out while debugging</span></span>

<span data-ttu-id="c795d-180">La ejecución paso a paso es el proceso de ejecutar una instrucción cada vez.</span><span class="sxs-lookup"><span data-stu-id="c795d-180">Stepping is the process of running one statement at a time.</span></span> <span data-ttu-id="c795d-181">Puede detenerse en una línea de código y examinar los valores de las variables y el estado del sistema.</span><span class="sxs-lookup"><span data-stu-id="c795d-181">You can stop on a line of code, and examine the values of variables and the state of the system.</span></span> <span data-ttu-id="c795d-182">En la tabla siguiente se describen las tareas de depuración comunes, como la depuración paso a paso por procedimientos, por instrucciones y para salir.</span><span class="sxs-lookup"><span data-stu-id="c795d-182">The following table describes common debugging tasks such as stepping over, stepping into, and stepping out.</span></span>

| <span data-ttu-id="c795d-183">Tarea de depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-183">Debugging Task</span></span> |                                                                                                                   <span data-ttu-id="c795d-184">Descripción</span><span class="sxs-lookup"><span data-stu-id="c795d-184">Description</span></span>                                                                                                                    |                                                      <span data-ttu-id="c795d-185">Cómo llevarla a cabo en PowerShell ISE</span><span class="sxs-lookup"><span data-stu-id="c795d-185">How to accomplish it in PowerShell ISE</span></span>                                                       |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span data-ttu-id="c795d-186">**Depurar paso a paso por instrucciones**</span><span class="sxs-lookup"><span data-stu-id="c795d-186">**Step Into**</span></span>  | <span data-ttu-id="c795d-187">Ejecuta la instrucción actual y, luego, se detiene en la instrucción siguiente.</span><span class="sxs-lookup"><span data-stu-id="c795d-187">Executes the current statement and then stops at the next statement.</span></span> <span data-ttu-id="c795d-188">Si la instrucción actual es una llamada de función o script, el depurador ejecuta la depuración paso a paso por instrucciones en la función o el script. De lo contrario, se detiene en la siguiente instrucción.</span><span class="sxs-lookup"><span data-stu-id="c795d-188">If the current statement is a function or script call, then the debugger steps into that function or script, otherwise it stops at the next statement.</span></span>                      | <span data-ttu-id="c795d-189">Presione <kbd>F11</kbd> o, en el menú **Depurar**, haga clic en **Paso a paso por instrucciones**. También puede escribir `S` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-189">Press <kbd>F11</kbd> or, on the **Debug** menu, click **Step Into**, or in the Console Pane, type `S` and press <kbd>ENTER</kbd>.</span></span>                 |
| <span data-ttu-id="c795d-190">**Depurar paso a paso por procedimientos**</span><span class="sxs-lookup"><span data-stu-id="c795d-190">**Step Over**</span></span>  | <span data-ttu-id="c795d-191">Ejecuta la instrucción actual y, luego, se detiene en la instrucción siguiente.</span><span class="sxs-lookup"><span data-stu-id="c795d-191">Executes the current statement and then stops at the next statement.</span></span> <span data-ttu-id="c795d-192">Si la instrucción actual es una llamada de función o script, el depurador ejecuta la función o el script completo y se detiene en la siguiente instrucción después de la llamada de función.</span><span class="sxs-lookup"><span data-stu-id="c795d-192">If the current statement is a function or script call, then the debugger executes the whole function or script, and it stops at the next statement after the function call.</span></span> | <span data-ttu-id="c795d-193">Presione <kbd>F10</kbd> o, en el menú **Depurar**, haga clic en **Paso a paso por procedimientos**. También puede escribir `V` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-193">Press <kbd>F10</kbd> or, on the **Debug** menu, click **Step Over**, or in the Console Pane, type `V` and press <kbd>ENTER</kbd>.</span></span>                 |
| <span data-ttu-id="c795d-194">**Depurar paso a paso para salir**</span><span class="sxs-lookup"><span data-stu-id="c795d-194">**Step Out**</span></span>   | <span data-ttu-id="c795d-195">Sale de la función actual y sube un nivel si la función está anidada.</span><span class="sxs-lookup"><span data-stu-id="c795d-195">Steps out of the current function and up one level if the function is nested.</span></span> <span data-ttu-id="c795d-196">Si se encuentra en el cuerpo principal, el script se ejecuta hasta el final o hasta el punto de interrupción siguiente.</span><span class="sxs-lookup"><span data-stu-id="c795d-196">If in the main body, the script is executed to the end, or to the next breakpoint.</span></span> <span data-ttu-id="c795d-197">Las instrucciones omitidas se ejecutan, pero no se depuran paso a paso.</span><span class="sxs-lookup"><span data-stu-id="c795d-197">The skipped statements are executed, but not stepped through.</span></span>                   | <span data-ttu-id="c795d-198">Presione <kbd>MAYÚS</kbd>+<kbd>F11</kbd> o, en el menú **Depurar**, haga clic en **Depurar paso a paso para salir**. También puede escribir `O` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-198">Press <kbd>SHIFT</kbd>+<kbd>F11</kbd>, or on the **Debug** menu, click **Step Out**, or in the Console Pane, type `O` and press <kbd>ENTER</kbd>.</span></span> |
| <span data-ttu-id="c795d-199">**Continuar**</span><span class="sxs-lookup"><span data-stu-id="c795d-199">**Continue**</span></span>   | <span data-ttu-id="c795d-200">Continúa la ejecución hasta el final o hasta el punto de interrupción siguiente.</span><span class="sxs-lookup"><span data-stu-id="c795d-200">Continues execution to the end, or to the next breakpoint.</span></span> <span data-ttu-id="c795d-201">Las funciones omitidas y las invocaciones se ejecutan, pero no se ejecutan paso a paso.</span><span class="sxs-lookup"><span data-stu-id="c795d-201">The skipped functions and invocations are executed, but not stepped through.</span></span>                                                                                                          | <span data-ttu-id="c795d-202">Presione <kbd>F5</kbd> o, en el menú **Depurar**, haga clic en **Ejecutar o continuar**. También puede escribir `C` en el panel de consola y presionar <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-202">Press <kbd>F5</kbd> or, on the **Debug** menu, click **Run/Continue**, or in the Console Pane, type `C` and press <kbd>ENTER</kbd>.</span></span>               |

## <a name="how-to-display-the-values-of-variables-while-debugging"></a><span data-ttu-id="c795d-203">Cómo mostrar los valores de variables durante la depuración</span><span class="sxs-lookup"><span data-stu-id="c795d-203">How to display the values of variables while debugging</span></span>

<span data-ttu-id="c795d-204">Puede mostrar los valores actuales de las variables en el script mientras realiza la depuración paso a paso del código.</span><span class="sxs-lookup"><span data-stu-id="c795d-204">You can display the current values of variables in the script as you step through the code.</span></span>

### <a name="to-display-the-values-of-standard-variables"></a><span data-ttu-id="c795d-205">Para mostrar los valores de las variables estándar</span><span class="sxs-lookup"><span data-stu-id="c795d-205">To display the values of standard variables</span></span>

<span data-ttu-id="c795d-206">Utilice uno de los métodos siguientes:</span><span class="sxs-lookup"><span data-stu-id="c795d-206">Use one of the following methods:</span></span>

- <span data-ttu-id="c795d-207">En el panel de scripts, mantenga el puntero sobre la variable para mostrar su valor como una información sobre herramientas.</span><span class="sxs-lookup"><span data-stu-id="c795d-207">In the Script Pane, hover over the variable to display its value as a tool tip.</span></span>

- <span data-ttu-id="c795d-208">En el panel de consola, escriba el nombre de la variable y presione <kbd>ENTRAR</kbd>.</span><span class="sxs-lookup"><span data-stu-id="c795d-208">In the Console Pane, type the variable name and press <kbd>ENTER</kbd>.</span></span>

<span data-ttu-id="c795d-209">Todos los paneles de ISE están siempre en el mismo ámbito.</span><span class="sxs-lookup"><span data-stu-id="c795d-209">All panes in ISE are always in the same scope.</span></span> <span data-ttu-id="c795d-210">Por lo tanto, mientras está depurando un script, los comandos que se escriben en el panel de consola se ejecutan en el ámbito del script.</span><span class="sxs-lookup"><span data-stu-id="c795d-210">Therefore, while you are debugging a script, the commands that you type in the Console Pane run in script scope.</span></span> <span data-ttu-id="c795d-211">Esto le permite usar el panel de consola para buscar los valores de variables y llamar a funciones que solo se han definido en el script.</span><span class="sxs-lookup"><span data-stu-id="c795d-211">This allows you to use the Console Pane to find the values of variables and call functions that are defined only in the script.</span></span>

### <a name="to-display-the-values-of-automatic-variables"></a><span data-ttu-id="c795d-212">Para mostrar los valores de las variables automáticas</span><span class="sxs-lookup"><span data-stu-id="c795d-212">To display the values of automatic variables</span></span>

<span data-ttu-id="c795d-213">Puede usar el método anterior para mostrar el valor de casi todas las variables al depurar un script.</span><span class="sxs-lookup"><span data-stu-id="c795d-213">You can use the preceding method to display the value of almost all variables while you are debugging a script.</span></span> <span data-ttu-id="c795d-214">Sin embargo, estos métodos no funcionan con las siguientes variables automáticas.</span><span class="sxs-lookup"><span data-stu-id="c795d-214">However, these methods do not work for the following automatic variables.</span></span>

- `$_`

- `$Input`

- `$MyInvocation`

- `$PSBoundParameters`

- `$Args`

<span data-ttu-id="c795d-215">Si intenta mostrar el valor de cualquiera de estas variables, obtendrá el valor de esa variable en una canalización interna usada por el depurador, no el valor de la variable en el script.</span><span class="sxs-lookup"><span data-stu-id="c795d-215">If you try to display the value of any of these variables, you get the value of that variable for in an internal pipeline the debugger uses, not the value of the variable in the script.</span></span> <span data-ttu-id="c795d-216">Puede solucionar esto para algunas variables (`$_`, `$Input`, `$MyInvocation`, `$PSBoundParameters` y `$Args`) con el método siguiente:</span><span class="sxs-lookup"><span data-stu-id="c795d-216">You can work around this for a few variables (`$_`, `$Input`, `$MyInvocation`, `$PSBoundParameters`, and `$Args`) by using the following method:</span></span>

1. <span data-ttu-id="c795d-217">En el script, asigne el valor de la variable automática a una nueva variable.</span><span class="sxs-lookup"><span data-stu-id="c795d-217">In the script, assign the value of the automatic variable to a new variable.</span></span>

1. <span data-ttu-id="c795d-218">Para mostrar el valor de la nueva variable, mantenga el puntero sobre la nueva variable en el panel de scripts o escriba la nueva variable en el panel de consola.</span><span class="sxs-lookup"><span data-stu-id="c795d-218">Display the value of the new variable, either by hovering over the new variable in the Script Pane, or by typing the new variable in the Console Pane.</span></span>

<span data-ttu-id="c795d-219">Por ejemplo, para mostrar el valor de la variable `$MyInvocation`, en el script, asigne el valor a una nueva variable, como `$scriptName` y, a continuación, mantenga el puntero encima o escriba la variable `$scriptName` para mostrar su valor.</span><span class="sxs-lookup"><span data-stu-id="c795d-219">For example, to display the value of the `$MyInvocation` variable, in the script, assign the value to a new variable, such as `$scriptName`, and then hover over or type the `$scriptName` variable to display its value.</span></span>

```powershell
# In C:\ps-test\MyScript.ps1
$scriptName = $MyInvocation.MyCommand.Path
```

```PowerShell
# In the Console Pane:
.\MyScript.ps1
$scriptName
```

```Output
C:\ps-test\MyScript.ps1
```

## <a name="see-also"></a><span data-ttu-id="c795d-220">Consulte también</span><span class="sxs-lookup"><span data-stu-id="c795d-220">See Also</span></span>

- [<span data-ttu-id="c795d-221">Explorar Windows PowerShell ISE</span><span class="sxs-lookup"><span data-stu-id="c795d-221">Exploring the Windows PowerShell ISE</span></span>](exploring-the-windows-powershell-ise.md)
